{
  "name": "secret-channel-project",
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "lint": "prettier --check \"./js/*/(src|test|bench)/**/*.js\"",
    "format": "prettier --write \"./js/*/(src|test|bench)/**/*.js\""
  },
  "workspaces": [
    "./js/*"
  ],
  "devDependencies": {
    "prettier": "^3.1.0"
  }
}
